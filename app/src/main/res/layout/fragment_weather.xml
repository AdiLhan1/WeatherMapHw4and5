<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="20dp"
    android:background="@drawable/bg_weather_view">

    <ImageView
        android:id="@+id/ic_close"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:padding="18dp"
        android:src="@drawable/ic_close" />

    <TextView
        android:id="@+id/city_name_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="30dp"
        android:layout_marginTop="12dp"
        android:textColor="#FFF"
        android:textSize="@dimen/very_large_text_size"
        android:textStyle="bold"
        tools:text="Bishkek" />

    <TextView
        android:id="@+id/deg_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="30dp"
        android:layout_marginTop="-30dp"
        android:textColor="#FFF"
        android:textSize="@dimen/very_large_text_size"
        android:textStyle="bold"
        tools:text="+10" />

    <TextView
        android:id="@+id/info_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/deg_txt"
        android:layout_alignStart="@id/deg_txt"
        android:layout_marginTop="9dp"
        android:textColor="#FFF"
        tools:text="ясно" />

    <ImageView
        android:id="@+id/ic_weather_small"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_below="@id/info_txt"
        android:layout_alignStart="@id/info_txt"
        android:layout_marginStart="-12dp"
        android:layout_marginTop="18dp" />

    <TextView
        android:id="@+id/humidity_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/ic_weather_small"
        android:layout_alignBottom="@id/ic_weather_small"
        android:layout_marginStart="4dp"
        android:layout_marginTop="4dp"
        android:layout_toEndOf="@id/ic_weather_small"
        android:gravity="center"
        android:textColor="#FFF"
        android:textSize="16sp"
        tools:text="65% влажности" />

    <Button
        android:id="@+id/save_to_list_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginBottom="22dp"
        android:background="@drawable/bg_stroke"
        android:padding="12dp"
        android:text="@string/add_city_btn_txt"
        android:textAllCaps="false"
        android:textColor="@color/white" />

</RelativeLayout>